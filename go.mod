module github.com/Beeeeen/krinos

go 1.26

// Krinos ships with zero third-party dependencies by design.
//
// We are a supply-chain security tool. Every dependency we take is a
// dependency our users inherit, and an argument we lose. If this block is
// ever non-empty, it must be justified in ARCHITECTURE.md and approved in
// review — not added casually.
